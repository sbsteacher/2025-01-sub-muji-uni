spring:
  application:
    name: mujiuni
  datasource:
    url: jdbc:log4jdbc:mariadb://localhost/mujiuni
    username: root
    password: green502
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
  jackson:
    default-property-inclusion: non_null
  jpa:
    database: mysql
    defer-datasource-initialization: true
    hibernate:
      #ddl-auto: update
      ddl-auto: create
    properties:
      hibernate:
        jdbc:
          batch_size: 20
        format_sql: true
        order_updates: true
        show_sql: true